/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: Mar 04 2024 | 09:38:27 */
/* Default comment here */ 

jQuery(document).ready(function( $ ){
    // Your code in here
	$('.rec-job-info').append('<li class="mwb_li_appended"><a href="https://dev.cedcoss.com/wordpress/current-openings" class="rec-job-info-link"></a></li>');
	  // Select each <a> tag within an element with class "rec-job-title"
  $('.rec-job-title a').each(function() {
    // Get the href attribute of the <a> tag
    var link = $(this).attr('href');
    console.log( link );
    // Find the nearest <ul> with class "rec-job-info" and find the <a> with class "rec-job-info-link"
    $(this).closest('ul.rec-job-info').find('a.rec-job-info-link').attr('href', link);
  });
	//
	
	// remove first span 
  // Find all <li> elements with class starting with 'zrsite_'
//     $('li[class^="zrsite_"]').each(function(){
//         // Find the first <span> element within the <li> element
// 		console.log('test1234');
//         var firstSpan = $(this).find('span:first-child');
//         // Remove the first <span> element if found
//         if(firstSpan.length > 0) {
//             firstSpan.remove();
//         }
//     });
	// for pagination
// 	var itemsPerPage = 3;
//   var currentPage = 1;
  
//   // Hide all job cards
//   $('.rec-group > ul.rec-job-info').hide();
  
//   // Show the first set of job cards
//   $('.rec-group > ul.rec-job-info:lt(' + itemsPerPage + ')').show();
  
//   // Handle next button click
//   $('.next').click(function() {
//     var startIndex = currentPage * itemsPerPage;
//     var endIndex = startIndex + itemsPerPage;
    
//     // Hide all job cards
//     $('.rec-group > ul.rec-job-info').hide();
    
//     // Show the next set of job cards
//     $('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + startIndex + ')').show();
    
//     currentPage++;
//   });
  
//   // Handle previous button click
//   $('.prev').click(function() {
//     if (currentPage > 1) {
//       currentPage--;
      
//       var startIndex = (currentPage - 1) * itemsPerPage;
//       var endIndex = startIndex + itemsPerPage;
      
//       // Hide all job cards
//       $('.rec-group > ul.rec-job-info').hide();
      
//       // Show the previous set of job cards
//       $('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + startIndex + ')').show();
//     }
//   });
	
	
	// for pagination 2 
//  var itemsPerPage = 6;
//   var totalItems = $('.rec-group > ul.rec-job-info').length;
//   var totalPages = Math.ceil(totalItems / itemsPerPage);
//   var currentPage = 1;
// 	console.log(totalItems);
// 	console.log(totalPages);

//   // Hide all job cards
//   $('.rec-group > ul.rec-job-info').hide();

//   // Show the first set of job cards
//   $('.rec-group > ul.rec-job-info:lt(' + itemsPerPage + ')').show();

//   // Handle next button click
//   $('.next').click(function() {
//     if (currentPage < totalPages) {
//       var startIndex = currentPage * itemsPerPage;
//       var endIndex = startIndex + itemsPerPage;

//       // Hide all job cards
//       $('.rec-group > ul.rec-job-info').hide();

//       // Show the next set of job cards
//       $('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + (startIndex - 1) + ')').show();

//       currentPage++;
//     } else {
//       // If on the last page, loop back to the first page
//       currentPage = 1;
//       $('.rec-group > ul.rec-job-info:lt(' + itemsPerPage + ')').show();
//     }
//   });

//   // Handle previous button click
//   $('.prev').click(function() {
//     if (currentPage > 1) {
//       currentPage--;
// console.log(currentPage);
//       var startIndex = (currentPage - 1) * itemsPerPage;
//       var endIndex = startIndex + itemsPerPage;

//       // Hide all job cards
//       $('.rec-group > ul.rec-job-info').hide();

//       // Show the previous set of job cards
//       $('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + (startIndex - 1) + ')').show();
// 		console.log('abhu');
// 		console.log($('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + (startIndex - 1) + ')'));
//     } else {
//       // If on the first page, loop to the last page
//       currentPage = totalPages;
// 		console.log('abh2');
//       var startIndex = (currentPage - 1) * itemsPerPage;
//       var endIndex = startIndex + itemsPerPage;
      
//       // Hide all job cards
//       $('.rec-group > ul.rec-job-info').hide();
      
//       // Show the previous set of job cards
//       $('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + (startIndex - 1) + ')').show();
// 		console.log($('.rec-group > ul.rec-job-info:lt(' + endIndex + '):gt(' + (startIndex - 1) + ')'))
//     }
//   });
	
// Change text content when document is ready
//     $(".zrsite_Job_Type span:first-child").text("Application");
    $(".zrsite_City span:first-child").text("Location:");
	
	
});